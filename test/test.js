let { assert, expect, should } = require('chai');
// Test for correct relationships management

// Test to fetch the users

// Test to fetch the Organisations

// Test to fetch the Tickets

// Test to see if I get/ don't get the tickets for an user

// Test to see if I get/ don't get the tickets for an organisation
describe('Array', function () {
    describe('#indexOf()', function () {
        it('should return -1 when the value is not present', function () {
            assert.equal([1, 2, 3,].indexOf(4), -1);
        });
    });
});